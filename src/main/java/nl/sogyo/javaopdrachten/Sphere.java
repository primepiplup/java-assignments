package nl.sogyo.javaopdrachten;

public class Sphere implements Shape{
    private Vector origin;
    private double radius;

    public Vector[] intersect(Line line) {
        Vector[] intersectPoints = new Vector[0];
        return intersectPoints;
    }
}
